services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: goldierillcn-dev
    ports:
      - "5933:5933"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
    restart: unless-stopped
